// Command tester runs a full single-node pipeline locally (ingest -> test ->
// output) against a fixture list. It is the Phase 0 entrypoint. See DESIGN.md.
package main

import "fmt"

func main() {
	// Wiring is added in T0.8 (engine).
	fmt.Println("vless-tester local runner: not implemented yet")
}
